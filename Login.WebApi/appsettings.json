{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cors": {
    "AllowedOrigins": "http://localhost:4200,https://localhost:4200, https://lemon-desert-081687a1e.4.azurestaticapps.net"
  },
  "ConnectionStrings": {
    // No poner la cadena real aquí (este archivo va al repo). Configúrala con:
    // dotnet user-secrets set "ConnectionStrings:DefaultConection" "Server=...;Port=3306;Database=...;User=...;Password=...;"
    // o como variable de entorno en el servidor de despliegue.
    "DefaultConection": ""
  },
  "Jwt": {
    "Issuer": "Login.WebApi",
    "Audience": "Login.Frontend",
    "Key": "FAUsTi7hAWHpuBFGkMbfe8Ub139NHA1f",
    "ExpiresMinutes": 60
  },
  "GoogleDrive": {
    // No poner las credenciales reales aquí (este archivo va al repo).
    // Configúralas con:
    //   dotnet user-secrets set "GoogleDrive:OAuthClientId" "..."
    //   dotnet user-secrets set "GoogleDrive:OAuthClientSecret" "..."
    // o como variables de entorno en el servidor de despliegue. La conexión
    // en sí (refresh_token + carpeta raíz) se guarda en la tabla
    // GoogleDriveConnection, no aquí.
    "OAuthClientId": "",
    "OAuthClientSecret": "",
    // A dónde redirigir el navegador después de que Google confirme la
    // conexión (pantalla del frontend que muestra el resultado).
    "FrontendRedirectUrl": "http://localhost:4200/security/google-drive"
  }
}
